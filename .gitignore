# Python
__pycache__/
*.pyc
.venv/
venv/

# Env
.env
*.env

# OS/IDE
.DS_Store
.vscode/*
!.vscode/settings.json

# Local-only helpers
.local/

# Cloudflare local build cache
.wrangler/
cloudflare-worker/.wrangler/

__pycache__/
*.pyc
.venv/
venv/
.env
*.env

# Oversized / unused 3D assets — Cloudflare Pages hard-fails at 25 MiB per file
3dprint-modules/3dmodels/TX-a-vintage-film-camera-pr-550.glb
img/duck.glb.orig-bak

# Oversized showcase models — Cloudflare Pages hard-fails at 25 MiB per file
3D Workspace Models/TX-a-full-body-3d-digital-s-102.glb
3D Workspace Models/TX-a-highly-stylized-humoro-111.glb
3D Workspace Models/TX-a-male-dragonborn-barbar-235.glb

# Full-resolution model masters. Not served — the optimised cuts beside them
# are. Five of these are over the 25 MiB ceiling (largest 76.8 MiB), so a
# stray `git add -A` here fails the Pages build rather than the commit.
# Mirrors the mono repo's own rule for the same directory.
3D Workspace Models/_source-originals/
