# ============================================================
# THIS FILE IS IGNORED — routing is handled by _worker.js
# ============================================================
# Cloudflare Pages Advanced Mode (_worker.js present) ignores
# _redirects and _headers. All routing logic lives in _worker.js.
#
# This file is kept for documentation only.
#
# Clean URL rewrites:   /hub → hub.html       (via env.ASSETS.fetch)
# Canonical redirects:  /hub.html → /hub      (301)
# Trailing slashes:     /hub/ → /hub          (301)
# ============================================================
